(function () {
    "use strict";

    // Placeholder router for user dashboard pages.
    // The current dashboard uses direct HTML navigation.
    window.userDashboardRouter = window.userDashboardRouter || { ready: true };
})();
