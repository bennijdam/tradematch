services:
  - type: web
    name: tradematch-backend
    env: node
    buildCommand: cd public && npm install && npm run build
    startCommand: cd public && npm start
    staticPublishPath: public
    routes:
      - path: /services/*
        directory: ./services
