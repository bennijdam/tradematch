(function () {
    "use strict";

    // Placeholder router for vendor dashboard pages.
    // The current dashboard uses direct HTML navigation.
    window.vendorDashboardRouter = window.vendorDashboardRouter || { ready: true };
})();
